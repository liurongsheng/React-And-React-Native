# node 使用

```ts
mkdir my-ts-node
cd my-ts-node
npm init -y

npm install -D typescript ts-node @types/node

npm install -D cross-env

npm install -D tslib 
```

```text
npm 简写说明
-D === --save-dev
```
